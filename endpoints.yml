# This file contains the different endpoints your bot can use.

# Action server (custom actions) - default Rasa action server runs on port 5055
action_endpoint:
  url: "http://localhost:5055/webhook"

# (Optional) Model server configuration - uncomment and edit if you use remote model storage
# models:
#   url: http://my-server.com/models/default_core@latest
#   wait_time_between_pulls: 10

# Tracker store and event broker configurations can go here if needed.

# Rasa Enterprise (talk) endpoint (if used)
rasa:
  url: "http://localhost:5002/api"
